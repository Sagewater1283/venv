#!/usr/bin/python3


hostname= input('what value should we set for hostname?')

##Here we use the str.lower() method to returna lowercase string
if hostname.lower() == 'mtg':
    print('the hostname was found to be mtg')

